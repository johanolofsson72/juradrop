{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Bootstrap capability set — deny-by-default. No plugins, no core APIs exposed to the WebView. Every later spec MUST opt in explicitly by adding a permission entry here or in a new scoped capability file. Per FR-019 and research.md R-002.",
  "windows": ["main"],
  "permissions": []
}
