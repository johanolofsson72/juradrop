{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Spec 002 — adds shell sidecar (scoped to bundled ollama binary) and event/app core permissions. No filesystem, no http, no shell beyond the bundled binary. Per FR-016 / spec 002 contracts/capabilities.md.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:app:default",
    "core:event:default",
    {
      "identifier": "shell:allow-spawn",
      "allow": [
        { "name": "binaries/ollama", "sidecar": true, "args": true }
      ]
    },
    "shell:allow-kill"
  ]
}
