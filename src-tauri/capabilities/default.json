{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Spec 002 — adds shell sidecar (scoped to bundled ollama binary) and event/app core permissions. Spec 010 — adds shell.open scoped to the GitHub Releases URL for the About-section link. No filesystem, no http, no shell beyond the bundled binary and the scoped URL.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:app:default",
    "core:event:default",
    {
      "identifier": "shell:allow-spawn",
      "allow": [
        { "name": "binaries/ollama", "sidecar": true, "args": true }
      ]
    },
    "shell:allow-kill",
    {
      "identifier": "shell:allow-open",
      "allow": [
        { "url": "https://github.com/johanolofsson72/juradrop/releases" }
      ]
    }
  ]
}
