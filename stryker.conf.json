{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "Mutation-test gate for JuraDrop frontend logic. The privacy-critical logic lives in Rust (cargo-mutants); this targets the TS logic modules (stores, estimator, identity map, bridge). Pure-UI components are excluded — mutating JSX yields low-signal survivors. Run: npx stryker run",
  "packageManager": "npm",
  "testRunner": "vitest",
  "vitest": {
    "configFile": "vitest.config.ts"
  },
  "coverageAnalysis": "perTest",
  "mutate": [
    "src/lib/drag-hover.ts",
    "src/lib/settings-store.ts",
    "src/lib/status-store.ts",
    "src/lib/tier-download-store.ts",
    "src/lib/use-progress-estimate.ts",
    "src/components/DropZone.identity.ts"
  ],
  "thresholds": {
    "high": 80,
    "low": 70,
    "break": null
  },
  "tempDirName": ".stryker-tmp",
  "concurrency": 4,
  "timeoutMS": 60000
}
